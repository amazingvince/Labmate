# secrets — NEVER commit the API key (repo is public)
.env
*.env
.envrc

# python
__pycache__/
*.pyc
.venv/
venv/
.pytest_cache/

# per-worktree Claude Code wiring (created by the runbook, not part of the product)
.claude/settings.json
.claude/settings.local.json

# os / editor
.DS_Store
.idea/
.vscode/

# scratch
/tmp/
node_modules/
